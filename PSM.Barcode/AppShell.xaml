<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PSM.Barcode.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:views="clr-namespace:PSM.Barcode.Views"
	Shell.FlyoutBehavior="Disabled"
    Title="PSM.Barcode"
>
	<ShellContent Title="Home" ContentTemplate="{DataTemplate views:MainPage}" Route="MainPage" />
	<!--<TabBar>
		<ShellContent Title="Home" ContentTemplate="{DataTemplate views:MainPage}" Route="MainPage" />
		<ShellContent Title="Login" ContentTemplate="{DataTemplate views:LoginPage}" Route="LoginPage" />
		<ShellContent Title="Barcodes" ContentTemplate="{DataTemplate views:BarcodesPage}" Route="BarcodesPage" />
	</TabBar>-->
</Shell>
